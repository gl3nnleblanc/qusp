language: julia

jobs:
  include:
    - stage: "Testing"
      julia: 1.5.3
      os: linux
      script:
        - ls
        - ls test/
        - julia test/test_mps.jl
        - julia test/test_tebd.jl
      after_success: skip
